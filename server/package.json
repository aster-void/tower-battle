{
  "name": "server",
  "scripts": {
    "dev": "bun run --watch ./index.ts",
    "check": "bunx tsc"
  },
  "dependencies": {
    "hono": "^4.6.20",
    "core": "file:../core"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}